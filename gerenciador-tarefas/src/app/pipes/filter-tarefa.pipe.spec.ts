import { FilterTarefaPipe } from './filter-tarefa.pipe';

describe('FilterTarefaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTarefaPipe();
    expect(pipe).toBeTruthy();
  });
});
